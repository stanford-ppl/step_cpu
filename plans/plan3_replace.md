Add an argument to /home/ginasohn/research/mocha/darpa/modified/causal_language_modeling.py so that I can choose whether I want to run it with the default PyTorch implementation or replace GPT2MLP with the implementation generated by the code generator you wrote in /home/ginasohn/research/mocha/step.
(In the future, we might have more modules we want to replace so design it with this in mind).

Add tests to compare how long it takes and checks whether the results are identical.

To run the tests, use
```
conda activate py312clean
source /home/ginasohn/research/mocha/.venv/bin/activate
```